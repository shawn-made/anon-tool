# AnonTool Environment Configuration
# Copy this file to .env and adjust if needed:
#   cp .env.example .env

# ============================================================
# Ollama (optional — tool works without it)
# ============================================================

# Ollama API URL (default: local installation)
OLLAMA_URL=http://localhost:11434

# Ollama model for name verification (default: llama3.1:8b)
OLLAMA_MODEL=llama3.1:8b

# ============================================================
# Paths (defaults to ~/.anontool/)
# ============================================================

# Override base directory (optional)
# ANONTOOL_HOME=~/.anontool
